# ── Workspace-level env vars ──────────────────────────────────────────────
# Copy to .env and fill in values. Never commit .env.

# MCP / tooling
GITHUB_TOKEN=ghp_your_token_here

# ── Service-level vars (also duplicated in each app's .env.example) ──────
# These are here for reference. Each app has its own .env.example.
